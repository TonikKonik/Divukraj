name: Deploy to Hetzner

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Write .env.local
        uses: appleboy/ssh-action@v1.0.3
        env:
          DB_URL: ${{ secrets.DIVUKRAJ_DATABASE_URL }}
          AUTH_SECRET: ${{ secrets.DIVUKRAJ_AUTH_SECRET }}
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          envs: DB_URL,AUTH_SECRET
          script: |
            printf "DATABASE_URL=%s\nAUTH_SECRET=%s\n" \
              "$DB_URL" "$AUTH_SECRET" > /var/www/divukraj/.env.local
            printf "DATABASE_URL=%s\n" \
              "$DB_URL" > /var/www/divukraj/.env
            echo env written

      - name: Deploy
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          script: |
            set -e
            cd /var/www/divukraj
            git fetch origin main && git reset --hard origin/main
            if [ ! -f package.json ]; then
              echo "No package.json – app not initialized yet, skipping deploy."
              exit 0
            fi
            pm2 delete divukraj 2>/dev/null || true
            npm ci
            [ -f prisma/schema.prisma ] && npx prisma generate && npx prisma db push --accept-data-loss || true
            npm run build
            pm2 start pm2.config.js && pm2 save
            echo deploy done